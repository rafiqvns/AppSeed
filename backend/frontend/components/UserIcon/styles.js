import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  starIcon: {
    color: "#F7B500",
    fontSize: 16,
  },
  nameContainer: {
    marginLeft: 5,
  },
  noWrap: {
    flexWrap: "nowrap",
  },
  wContent: {
    minWidth: "max-content",
  },
}));
